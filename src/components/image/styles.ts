import {StyleSheet} from 'react-native';
import {colors} from '../../theme';
const styles = StyleSheet.create({
  image: {backgroundColor: colors.black, resizeMode: 'contain'},
});
export default styles;
